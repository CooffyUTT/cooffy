info:
  name: Delete user
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8000/api/auth/pk/
  auth:
    type: bearer
    token: "{{access_token}}" 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
