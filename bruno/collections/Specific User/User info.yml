info:
  name: User info
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/api/auth/<pk>
  auth:
    type: bearer
    token: "{{access_token}}" 

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
