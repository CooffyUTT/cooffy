info:
  name: All schools
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/schools/
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
