info:
  name: Active Schools
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/schools/active/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
