info:
  name: All categories
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/menu/categories
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
