info:
  name: All Orders
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/orders/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
