info:
  name: Add Product to Order
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/orders/product/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
