info:
  name: All Client Orders
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/orders/?client_id=1
  params:
    - name: client_id
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
