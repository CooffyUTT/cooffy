info:
  name: Create Order
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/orders/
  body:
    type: json
    data: |-
      {
        "order_number": 1001,
        "date": "2026-07-24",
        "branch_id": 1,
        "client_id": 1,
        "payment_method": 1,
        "comment": "orden de prueba"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
