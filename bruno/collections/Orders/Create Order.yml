info:
  name: Create Order
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8000/api/orders/
  body:
    type: json
    data: |-
      {
        "branch_id": 1,
        "client_id": 1,
        "payment_method": 1,
        "comment": "Orden de prueba",
        "order_products": [
          {"item_id": 1, "quantity": 2}
        ]
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
