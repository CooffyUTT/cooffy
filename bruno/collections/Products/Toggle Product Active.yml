info:
  name: Toggle Product Active
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:8000/api/menu/manage/products/1/toggle-active/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
