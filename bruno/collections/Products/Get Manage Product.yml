info:
  name: Get Manage Product
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/menu/manage/products/1
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
