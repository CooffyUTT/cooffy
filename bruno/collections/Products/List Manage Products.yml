info:
  name: List Manage Products
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/menu/manage/products/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
