info:
  name: Menu Product
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/menu/products/1
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
