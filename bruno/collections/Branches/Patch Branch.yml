info:
  name: Patch Branch
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:8000/api/branches/1/
  body:
    type: form-urlencoded
    data:
      - name: name
        value: "Cafetería Actualizada"
        type: text
      - name: location
        value: "Edificio A, Primer Piso"
        type: text
      - name: schedule
        value: "09:00 - 18:00"
        type: text
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
