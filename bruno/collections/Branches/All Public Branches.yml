info:
  name: All Public Branches
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8000/api/branches/public/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
