info:
  name: School Companies
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/api/branches/companies/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
