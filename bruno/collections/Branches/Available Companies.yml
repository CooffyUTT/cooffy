info:
  name: Available Companies
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8000/api/branches/companies/available/
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
