info:
  name: Branch
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/branches/1
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
