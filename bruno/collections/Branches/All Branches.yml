info:
  name: All Branches
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/branches/
  params:
    - name: company_id
      value: "1"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
