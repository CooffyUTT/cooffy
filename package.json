{
    "scripts": {
        "front": "pnpm --dir frontend dev",
        "back": "cd backend && uv run python -u manage.py runserver",
        "dev": "concurrently -n back,front \"pnpm back\" \"pnpm front\"",
        "front:install": "pnpm --dir frontend install",
        "back:install": "cd backend && uv run pip install -r requirements.txt",
        "install:all": "pnpm install && pnpm front:install && pnpm back:install"
    },
    "devDependencies": {
        "concurrently": "^10.0.3"
    }
}